get_filename_component(TURINGFORGE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${TURINGFORGE_CMAKE_DIR})

if(NOT TARGET Turingforge::Turingforge)
    include("${TURINGFORGE_CMAKE_DIR}/TuringforgeTargets.cmake")
endif()

set(TURINGFORGE_lIBRARIES Turingforge::Turingforge)
